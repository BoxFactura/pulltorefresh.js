extend ../layout

block setup
  - basedir = '..'

block run
  script.
    /* global PullToRefresh */
    PullToRefresh.init({
      mainElement: '#main',
      onRefresh: function() { alert('refresh') }
    });
