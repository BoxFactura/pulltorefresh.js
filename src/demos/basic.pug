extend _layout

block run
  script.
    /* global PullToRefresh */
    PullToRefresh.init({
      bodyElement: '#main',
      triggerElement: '.trigger',
      refreshFunction: () => alert('OSOM'),
    });
